Ideas:
- import/export saved house list
- game stats ← stats accumulation implemented; display UI still needed
- show icon in the addons option list
- reset stats closes stats view
- logged in and game session was still active
- the previous game modes gets cleared on /reload


TO be tested:
- sardines
- hot potato (/reload breaks seeker) (/reload resets the seeker list to remove duplicates)
- update bloodhound a bit, maybe colored arrow, or maybe the player gets a raid marker above their head 
- unselecting recent rounds between reloads or transitions